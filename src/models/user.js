export class User {
    constructor(
        id,
        name,
        username,
        password,
        birthday
    ) {
        this.id = id;
        this.name = name;
        this.username = username;
        this.password = password;
        this.birthday = birthday;
    }
}